[package]
name = "agent-core"
version = "0.1.0"
edition = "2024"
description = "Lightweight Codex and Claude session monitor for Agent Island"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
